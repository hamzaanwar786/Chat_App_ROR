<h1>New Message</h1>

<%= form_with(model: [@message.room, @message]) do |form| %>
  <%= form.label :content %>
  <%= form.text_field :content %>
  <%= form.submit "Send" %>
<% end %>

<%= link_to 'Back', @message.room %>